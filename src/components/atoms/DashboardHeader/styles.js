import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    position: "absolute",
    right: 15,
    top: 7,
  },
  container: {
    flex: 1,
  },
});
export default styles;
